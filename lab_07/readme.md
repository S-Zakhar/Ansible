### Tasks:

```
1) Написать диагностическую роль, которая будет проверять как настроена инфраструктура:
1.1) Убедиться, что mysql запущен (либо порт отвечает)
1.2) Убедиться, что nginx запущен (либо порт отвечает)
1.3*) Запросить у nginx любой статический файл и проверить,
что он скачивается корректно, сверив checksum

2) Модифицировать роль из предыдущего заняти по деплою nginx: добавить статический
файл с health-информацией (либо взять пример с вебинара,
либо реализовать отдельный url на nginx) /health

3) Добавить в роль проверки nginx и mysql проверку условий /health (как в вебинаре):
если true (все ок), тогда ничего не делаем, если false, тогда запускаем доп. таски диагностики

4*) Модифицировать роль деплоя nginx (из предыдущего занятия) таким образом, чтобы
nginx мог деплоиться как в докере, так и пакетом на виртуалку.
В инвентори выделить под это отдельный параметр nginx.deploy_type: ["docker","vm"].
nginx.deploy_type поле проверять на валидность
4.1*) при деплое в докере создать на хосте каталог с конфигами, каталог с логами
```